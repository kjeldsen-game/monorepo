name: Manual Echo

# This allows the workflow to be triggered manually from GitHub UI
on:
  workflow_dispatch:

jobs:
  echo_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Echo something
        run: echo "Hello! This job was triggered manually."
