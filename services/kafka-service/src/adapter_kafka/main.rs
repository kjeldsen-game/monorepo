use kafka::client::metadata::TopicNames;

pub fn get_current_topic_names() -> TopicNames {
    
}

fn get_client() {
    dotenv().ok(); // Load environment variables
   
    use std::fmt;i

    use std::fmt;
    
use std::fmt::write;
use std::fmt::write;
}

